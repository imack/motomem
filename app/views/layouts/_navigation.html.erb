<%= image_tag(asset_path("logo.png"), :class=>"motomem")%>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>        
    </li>
  <% else %>
    <li>
    <%= link_to 'Login', user_omniauth_authorize_path(:facebook) %>
    </li>
  <% end %>
</ul>